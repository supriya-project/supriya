language: python
python:
  - "2.7"
  - "3.4"
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y supercollider
  - sudo apt-get install -y graphviz
script:
  - python setup.py build
  - python setup.py install
  - run-supriya-doctests