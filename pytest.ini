[pytest]
addopts =
    --cov-branch
    --cov-report=html
    --cov-report=term
    --doctest-modules
    --durations=20
    -vv
doctest_optionflags =
    ELLIPSIS
    NORMALIZE_WHITESPACE
testpaths =
    tests
    supriya
